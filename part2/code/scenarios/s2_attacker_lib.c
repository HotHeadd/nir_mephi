/*
Библиотека-нарушитель для сценария 2.
Содержит функцию, которая пишет в чужую критическую переменную.
Цель сценария: показать, что атрибуция определяет МОДУЛЬ-инициатор
(эту .so), а не основную программу.
*/
#include <stdint.h>

// пишет переданным адресом — флаг живёт в основной программе
void attacker_write(volatile uint64_t *target) {
    *target = 0xBADC0DE;   // недопустимая запись из чужого модуля
}
